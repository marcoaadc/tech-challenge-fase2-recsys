# Copy to .env and adjust as needed. All values have sensible defaults.

# MLflow
MLFLOW_TRACKING_URI=mlruns
MLFLOW_EXPERIMENT_NAME=recsys-ecommerce

# Reproducibility
SEED=42

# Paths (relative to project root)
DATA_DIR=data
MODELS_DIR=models
REPORTS_DIR=reports
